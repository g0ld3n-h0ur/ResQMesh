# app/dependencies/__init__.py
# FastAPI dependency callables for the ResQMesh platform.
#
# Import from specific modules (e.g. app.dependencies.auth) to avoid
# loading all dependencies eagerly at startup.
