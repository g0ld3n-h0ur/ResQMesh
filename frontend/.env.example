# Frontend API base URL (must match where the FastAPI backend is running)
# Local dev:
VITE_API_BASE_URL=http://localhost:8000/api/v1

# If the frontend runs on another machine on your LAN, use the backend host IP:
# VITE_API_BASE_URL=http://192.168.1.100:8000/api/v1
